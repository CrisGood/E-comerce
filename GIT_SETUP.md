# Configuración Git para las prácticas

## Practica1 — HTML y CSS
```bash
cd "c:\Users\Cristian\Documents\e-comerce\Practica1"
git init
git add .
git commit -m "Practica1: HTML y CSS"
git branch -M main
git remote add origin https://github.com/CrisGood/Practica1.git
git push -u origin main
```

## Practica2 — Imágenes 3D y realidad virtual
```bash
cd "c:\Users\Cristian\Documents\e-comerce\Practica2"
git init
git add .
git commit -m "Practica2: 3D y VR"
git branch -M main
git remote add origin https://github.com/CrisGood/Practica2.git
git push -u origin main
```

## Practica3 — Conexión a PostgreSQL
```bash
cd "c:\Users\Cristian\Documents\e-comerce\Practica3"
git init
git add .
git commit -m "Practica3: Backend PostgreSQL"
git branch -M main
git remote add origin https://github.com/CrisGood/Practica3.git
git push -u origin main
```

> Si tu repositorio ya tiene otra rama principal (`master`), cambia `git branch -M main` por `git branch -M master` según corresponda.
